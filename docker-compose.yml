version: "2.1"

services:
  friend-management:
    image: friend-management
    container_name: friendmgt-app
    build:
          context: ./
          dockerfile: Dockerfile
    ports:
      - 80:8080
